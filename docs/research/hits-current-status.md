# HITS integration current status

- Existing scanner migrated in place.
- Four-byte `HITS` magic enforced.
- Header, grid, cell lists and `0x38` triangle-plane records parsed.
- Binary Inspector fields now expose flags, points, normal and plane D.
- Synthetic parser, Binary Inspector and ResourceAnalyzer fixtures migrated.
- No files owned by draft PR #26 were modified.
- GUI editor work remains after parser review and merge.
