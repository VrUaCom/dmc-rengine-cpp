# Phase Map

**Snapshot date:** 2026-08-25  
**Canonical base:** `main@8e67235fd26cf7af94146f4dc660eb49e3c1d133`

The project is tracked by subsystem gates and cross-layer dependencies, not one linear phase number. Canonical execution order: [GDSpaces L1/L2/L3 master roadmap](../gdspaces/master-roadmap.md).

## Foundation — maintained

C++20/CMake, Windows+Ubuntu CI, evidence/artifact identity, GDSpaces source/provenance, Binary Inspector, WorkingCopy and guarded output remain maintained foundations.

## GDSpaces L1 — ACTIVE PRIMARY

```text
receipt-bundle integrity (#212)
 -> recursive slot-path hardening (#210)
 -> direct-retail provenance
 -> representation classification
 -> real edit/rebuild/overlay/rematerialization
 -> original-game consumption + rollback (#209)
 -> final L1 audit
```

Main already contains artifact-stable acquisition, atomic publication and the product closure orchestrator. The remaining mandatory boundary is representative retail/original-process evidence, not another generic parser rewrite.

## GDSpaces L2 — ADVANCED SUPPORTING / NOT COMPLETE

Candidate construction, normalization, numbered-volume bootstrap/precedence and collision census are strong. Exact type-0 physical-provider final-open behavior and representative original-process selected-identity receipts remain open. L2 work is allowed when it supports the vertical L1 receipt.

## GDSpaces L3 — ADVANCED STATIC SPINE / NOT COMPLETE

FileSlot/AsyncIO, ZIP materialization, LoadedResource states and major claim/reset/release structure are strongly recovered at bounded scope. State-writer ownership, typed failure breadth and dynamic initial-load/transition/reset/shutdown receipts remain open.

## Downstream tools

Stage Ops owns stage/scene assembly over GDSpaces outputs; Stage Semantic Graph is derived from Stage Ops state; ModViz consumes it. None may create a private resource resolver/materializer. EXE Editor remains a parallel frontend over the Recovered Game Source Tree and binary evidence.

## Dependency ordering

```text
first L2-selected/L1-authored/L3-consumed retail receipt
 -> final L1 audit
 -> narrow L2 closure
 -> representative L3 lifecycle closure
 -> Stage Ops game-backed assembly
 -> semantic/editor verticals
 -> bounded recovered-subsystem equivalence
 -> progressive recompilation
```
