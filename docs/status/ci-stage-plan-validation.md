# DMC3 Stage Plan CI Validation

Scope:

- evidence-backed Phase 12 stage table descriptor;
- table offset/RVA/VA and 110×4 role validation;
- fixed `st001` four-role resource plan;
- case/slash/prefix-normalized matching over already enumerated GDSpaces resources;
- missing and ambiguous diagnostics;
- unique candidate conversion;
- `StageBundleAssembler` integration;
- all previous foundation, container, and Evidence import tests.

This validation does not parse the executable pointer table or claim a game-backed bundle. It validates the safe typed layer before that parser exists.
