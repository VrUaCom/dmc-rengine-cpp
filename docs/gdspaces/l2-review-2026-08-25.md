# GDSpaces Layer 2 — roadmap review — 2026-08-25

**Scope:** Layer 2 / Runtime Resolver only.

**Overall status:** ACTIVE / NOT COMPLETE.

This review supersedes the unresolved-L2-R1 status in `l2-audit-2026-08-24.md` where direct instruction evidence recovered on 2026-08-25 is more recent. The older audit remains valid for the already-recovered candidate order, archive lookup, bootstrap precedence, ownership corrections, product-policy boundaries and still-open runtime/corpus validation gates.

## Roadmap checkpoint

| Order | Gate | Status | Review result |
| --- | --- | --- | --- |
| 1 | Exact type-0 physical provider after `0x0C` | **DONE — static reverse** | Direct canonical-EXE reverse recovered root join, final `CreateFileA` flags, ordinary miss errors, retry behavior and `FindFirstFileA` existence semantics. |
| 2 | Physical-provider model / receipt | **IN PROGRESS** | Instruction-backed `PhysicalProviderContract` and pure `PhysicalProviderModel` added. Current `product_physical_index` intentionally remains a portable product model until controlled parity receipt exists. |
| 3 | Real-retail `0x0E` collision census | **BLOCKED — ARTIFACT ACCESS** | Real Drive `dmc3-0.nbz` is identified (960,358,951 bytes), but the connected raw-download path rejects it above 268,435,456 bytes. No precomputed DMC3 retail central-directory census was found in the current evidence search. The separate real `DMCL-0.nbz` census already showed 1,095 unique normalized keys / zero collisions, but it is not a substitute for the DMC3 retail gate. |
| 4 | Direct-retail resolver receipt | **OPEN** | Need a real DMC3 retail request resolved to exact `ResourceRef` identity with candidate/provider/volume evidence. |
| 5 | `OpenGameResource` caller/fallback census | **DONE — canonical direct-call surface** | Whole-image direct-call census found exactly three direct callers, all passing flags `1`; that observed mode selects the generic six-prefix archive-before-physical resolver branch. Alternate internal flag branches are not promoted as active policy without separate indirect/runtime evidence. |
| 6 | `0x400` oversized-candidate behavior | **DONE — canonical direct-call surface** | The bounded candidate builder failure immediately destroys/releases the new file slot/object and returns `-1`; it does not continue to shorter prefixes or to the physical pass. |
| 7 | Controlled physical / missing / fallback receipts | **OPEN** | Static contract is available; controlled execution receipts still required. |
| 8 | Original-process selected-identity receipt | **OPEN** | Need original-process proof that selected identity matches the modeled resolver result. |
| 9 | Reconcile docs/issues/evidence | **IN PROGRESS** | L2-R1 evidence, caller/overflow census and evidence-class boundary are corrected in this branch; #204 now references the recovered physical-provider receipt. |
| 10 | Final Layer-2 audit / promotion | **OPEN** | Cannot promote until corpus + controlled runtime + original-process gates are closed and exact-head Windows + Ubuntu validation is green. |

## What remains confirmed from the previous Layer-2 audit

### Candidate construction and provider order

- lookup starts from the request basename;
- six prefixes are attempted in recovered order: `GDataX360.afs/`, `GData.afs/`, `Video/`, `afs/sound/`, `SAVEDATA/`, empty;
- archive attempts complete before physical attempts;
- archive normalization uses `0x0E`;
- physical normalization uses `0x0C`;
- the candidate attempt remains the outer precedence authority.

### Archive provider

- the original ZIP/NBZ backend owns a normalized sorted lookup representation;
- archive lookup uses the recovered `0x0E` qsort/bsearch path;
- `.index` files are not runtime authority;
- the recovered comparator has no semantic secondary tie-break for comparator-equal normalized keys;
- GDSpaces therefore preserves comparator-equal distinct identities as explicit ambiguity rather than inventing an original winner;
- the existing real `DMCL-0.nbz` census contains 1,095 unique `0x0E` normalized keys and zero collisions, but DMC3 retail still needs its own census.

### Volume bootstrap

- numbered `DMC3-%d.nbz` discovery is contiguous and first-gap bounded;
- physical mount is registered first;
- numbered archives are prepended, producing highest-contiguous-volume-to-zero lookup precedence;
- zero-volume bootstrap reaches the physical phase directly.

### Resolver ownership and fail-closed policy

- resolver indexes are derived from the exact currently mounted `ISource` enumeration rather than caller-owned stale index pointers;
- source mismatch / invalid resource / C-string-incompatible path / empty normalized key invalidate the source configuration;
- embedded-NUL requests fail before provider probing;
- archive/physical source-binding aliasing and duplicate archive volume/source bindings are rejected.

## New 2026-08-25 correction: physical provider

The previous blocker said the exact post-`0x0C` final physical open/comparison behavior was not directly recovered. That statement is now obsolete.

Direct reverse of the canonical executable establishes:

- type-0 mount registration at `0x140326D20` with normalization flags `0x0C`;
- physical resolution at `0x140327430`;
- bounded root join at `0x1403272C0`;
- existence check at `0x140327720` through `FindFirstFileA` / `FindClose`;
- final open at `0x140327800` through `CreateFileA(path, GENERIC_READ, FILE_SHARE_READ, NULL, OPEN_EXISTING, 0, NULL)`;
- `ERROR_FILE_NOT_FOUND` and `ERROR_PATH_NOT_FOUND` are normal open misses;
- `ERROR_NO_MORE_FILES`, `ERROR_FILE_NOT_FOUND` and `ERROR_PATH_NOT_FOUND` are normal existence-check misses;
- other errors are retried;
- no additional game-side case-folding stage was found after `0x0C` before the Win32 path APIs.

The implementation consequence is deliberately conservative: knowing the original direct-open contract does **not** make the existing source-enumeration/index implementation mechanically identical to it.

## New 2026-08-25 correction: OpenGameResource caller and overflow surface

A whole-image direct-call census for `OpenGameResource 0x14002FCA0` found three direct calls at `0x14003340A`, `0x1403380C7` and `0x1403381F7`. Every caller loads `EDX = 1` immediately before the call.

For that recovered caller mode:

- basename extraction and the six-prefix table are the active lookup policy;
- archive mask `1` runs before physical mask `2`;
- every candidate is formed through `0x1403272C0` with capacity `0x400`;
- candidate-construction failure cleans up the newly allocated file slot/object and returns `-1` immediately;
- overflow does not skip to a shorter prefix and does not proceed to physical lookup.

The first prefix `GDataX360.afs/` is also the longest (14 bytes), so the existing GDSpaces whole-plan fail-closed length validation is equivalent to this canonical direct-call behavior: if that first candidate cannot fit with its terminating NUL, the original runtime already aborts before any shorter prefix can be considered.

The detailed receipt is `l2-open-game-resource-census-2026-08-25.md`.

## Current promotion boundary

Layer 2 must not be called complete yet. The shortest evidence-backed path from this review is:

1. finish the controlled physical-provider model/receipt without relabeling the product index as original-equivalent;
2. acquire a usable DMC3 retail central-directory/member-list artifact and run the `0x0E` collision census;
3. capture a direct-retail resolver receipt;
4. capture controlled physical/missing/fallback receipts;
5. capture original-process selected-identity evidence;
6. reconcile all Layer-2 docs/issues/evidence;
7. run exact-head Windows + Ubuntu validation;
8. perform final Layer-2 audit and only then promote.

No Layer-3/Stage-Ops work is part of this checkpoint.
