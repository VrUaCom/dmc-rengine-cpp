{
  "schema": "dmc-rengine.mod-unknown-field-receipt.v2",
  "field": "mesh+0x0C",
  "serialized_offset": "mesh +0x0C",
  "width": "u32",
  "canonical_executable": {
    "name": "dmc3.exe",
    "sha256": "e454272ed0fb0247fcbcf300e5d55d7a3e96d50b89b9ffaff81bb978dcbdd082"
  },
  "corpus": {
    "mod_meshes": 180,
    "mod_zero_values": 180,
    "efm_meshes": 2,
    "efm_zero_values": 2,
    "status": "CORPUS_CONFIRMED"
  },
  "mod_mesh_provenance": {
    "record_stride": "0x50",
    "post_load_record_base": "0x1402FE452..0x1402FE462 derives object.mesh_table + mesh_index*0x50",
    "runtime_builder_record_base": "0x1402FE6F4..0x1402FE707 derives the same 0x50-byte source mesh record",
    "size_planner_record_base": "0x1402FDE54..0x1402FDE70 derives object.mesh_table + mesh_index*0x50 in the MOD layout planning path",
    "status": "EXE_CONFIRMED"
  },
  "provenance_confirmed_field_access_census": {
    "post_load_0x1402FE3B0": {
      "positive_offsets": ["+0x10", "+0x18", "+0x20", "+0x28", "+0x30", "+0x40", "+0x48"],
      "mesh_plus_0c_accesses": 0
    },
    "runtime_builder_0x1402FE6A0": {
      "positive_offsets": ["+0x00", "+0x02", "+0x10", "+0x18", "+0x20", "+0x28", "+0x30", "+0x40", "+0x48"],
      "mesh_plus_0c_accesses": 0
    },
    "layout_planning_path_near_0x1402FDE64": {
      "positive_offset": "+0x00 vertex_count",
      "mesh_plus_0c_accesses": 0
    },
    "common_material_helper": "0x1402F9890 consumes mesh texture/clamp fields through +0x0A and establishes no +0x0C use",
    "status": "EXE_CONFIRMED"
  },
  "semantic_status": "PRESERVED_UNDECODED",
  "evidence_status": [
    "STRUCTURAL_CONFIRMED",
    "EXE_CONFIRMED",
    "CORPUS_CONFIRMED",
    "PRESERVED_UNDECODED"
  ],
  "rejected_hypotheses": [
    "mesh +0x0C is padding because it fills the gap before an aligned u64",
    "mesh +0x0C is reserved because the bounded corpus is zero",
    "mesh +0x0C may be zero-normalized by a writer",
    "any unrelated +0x0C access on a 0x50-byte structure is a MOD mesh consumer"
  ],
  "cpp_contract": "analysis/mod/mesh_serialized.hpp preserves raw u32; synthetic DEADBEEF regression is registered through mod_skin_tests",
  "writer_policy": "preserve",
  "closure_boundary": "The canonical MOD post-load, runtime-builder, layout-planning and material paths all have source-mesh provenance and skip +0x0C while consuming neighboring fields. This is strong EXE_CONFIRMED negative evidence for those complete MOD paths, but not authority to rename the serialized u32 reserved/padding. Preserve it exactly unless a distinct provenance-confirmed consumer is found."
}
