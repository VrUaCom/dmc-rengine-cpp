{
  "schema": "dmc-rengine.mod-unknown-field-receipt.v2",
  "field": "mesh+0x4C",
  "serialized_offset": "mesh +0x4C",
  "width": "u32",
  "canonical_executable": {
    "name": "dmc3.exe",
    "sha256": "e454272ed0fb0247fcbcf300e5d55d7a3e96d50b89b9ffaff81bb978dcbdd082"
  },
  "corpus": {
    "mod_meshes": 180,
    "mod_zero_values": 180,
    "efm_bound_meshes": 2,
    "efm_zero_values": 2,
    "status": "CORPUS_CONFIRMED"
  },
  "mod_mesh_provenance": {
    "record_stride": "0x50",
    "post_load_record_base": "0x1402FE452..0x1402FE462 derives object.mesh_table + mesh_index*0x50",
    "runtime_builder_record_base": "0x1402FE6F4..0x1402FE707 derives the same source record",
    "layout_planning_record_base": "0x1402FDE54..0x1402FDE70 derives object.mesh_table + mesh_index*0x50",
    "status": "EXE_CONFIRMED"
  },
  "neighbor_positive_control": {
    "post_load_write": "0x1402FE684 writes generated topology count to mesh +0x48",
    "runtime_builder_read": "0x1402FE904 reads mesh +0x48",
    "runtime_builder_write": "0x1402FE907 writes that count to runtime +0x198",
    "status": "EXE_CONFIRMED"
  },
  "provenance_confirmed_field_access_census": {
    "post_load_0x1402FE3B0": {
      "mesh_plus_4c_reads": 0,
      "mesh_plus_4c_writes": 0,
      "note": "function reaches the end-of-record +0x48 positive control and writes it, then advances to next 0x50-byte record without touching +0x4C"
    },
    "runtime_builder_0x1402FE6A0": {
      "mesh_plus_4c_reads": 0,
      "mesh_plus_4c_writes": 0,
      "note": "builder forwards +0x48 to runtime +0x198 and performs no +0x4C companion transfer"
    },
    "layout_planning_path_near_0x1402FDE64": {
      "mesh_plus_4c_reads": 0,
      "mesh_plus_4c_writes": 0
    },
    "status": "EXE_CONFIRMED"
  },
  "semantic_status": "PRESERVED_UNDECODED",
  "evidence_status": [
    "STRUCTURAL_CONFIRMED",
    "EXE_CONFIRMED",
    "CORPUS_CONFIRMED",
    "PRESERVED_UNDECODED"
  ],
  "rejected_hypotheses": [
    "mesh +0x4C is the second half of a runtime topology-count pair",
    "mesh +0x4C is padding because it ends the 0x50-byte record",
    "mesh +0x4C may be zero-normalized because current corpus is zero",
    "unrelated runtime/command structures with a +0x4C member are MOD serialized mesh evidence"
  ],
  "cpp_contract": "analysis/mod/mesh_serialized.hpp preserves raw u32; synthetic A5A55A5A regression is registered through mod_skin_tests",
  "writer_policy": "preserve",
  "closure_boundary": "Three provenance-confirmed canonical MOD paths derive the 0x50-byte source mesh record and skip +0x4C. The post-load and runtime-builder paths explicitly use neighboring +0x48 as a topology-count positive control, making absence of a +0x4C transfer stronger negative evidence. This still does not justify calling +0x4C padding/reserved; writer preservation remains mandatory."
}
